<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?android:attr/colorControlHighlight"> <!-- Uses system highlight color -->

    <!-- The actual WhatsApp-style camera shutter button -->
    <item>
        <layer-list >
            <!-- Outer Transparent Area -->
            <item>
                <shape android:shape="oval">
                    <solid android:color="@android:color/transparent"/>
                    <size android:width="80dp" android:height="80dp"/>
                </shape>
            </item>

            <!-- Outer White Ring -->
            <item>
                <shape android:shape="oval">
                    <solid android:color="@android:color/transparent"/> <!-- Transparent inner space -->
                    <stroke android:width="4dp" android:color="#FFFFFF"/> <!-- White ring -->
                </shape>
            </item>

            <!-- Inner White Circle -->
            <item android:width="48dp" android:height="48dp" android:gravity="center">
                <shape android:shape="oval">
                    <solid android:color="#FFFFFF"/> <!-- Solid white center -->
                </shape>
            </item>
        </layer-list>
    </item>

</ripple>
